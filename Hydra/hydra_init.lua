-- local hotkey = require("hotkey")
-- hotkey("D", {"cmd", "shift"}, function()
--    print("it works")
-- end):enable()

-- local app = require("app")
-- for k, v in pairs(app.running_apps()) do
--     if v:is_hidden() then print(v:title()) end
-- end

-- run user's script, if it exists
-- TODO: show alert if it doesn't
require("init")
